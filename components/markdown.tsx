export function Markdown({content}:{content:string}){const blocks=content.split(/\n\s*\n/);return <div className="prose">{blocks.map((block,i)=>{const text=block.trim();if(text.startsWith("## "))return <h2 key={i}>{text.slice(3)}</h2>;if(text.startsWith("### "))return <h3 key={i}>{text.slice(4)}</h3>;if(text.startsWith("- "))return <ul key={i}>{text.split("\n").map((x,j)=><li key={j}>{x.replace(/^- /,"")}</li>)}</ul>;return <p key={i}>{text.replace(/\*\*/g,"")}</p>})}</div>}
